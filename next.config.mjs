/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    //domain: ["images.unsplash.com"],
    domains: ["www.cnbctv18.com", "www.moneycontrol.com","auto.hindustantimes.com"],
  },
};

export default nextConfig;
